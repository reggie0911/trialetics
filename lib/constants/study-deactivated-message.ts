/** Returned from server actions when `studies.status === 'closed'`. */
export const STUDY_DEACTIVATED_MESSAGE =
  'This study is deactivated; changes are not allowed.';

/** Hint for disabled controls on study hub (matches study detail Edit tooltip tone). */
export const STUDY_DEACTIVATED_TOOLTIP =
  'Study is deactivated; changes are disabled until the study is reactivated.';
