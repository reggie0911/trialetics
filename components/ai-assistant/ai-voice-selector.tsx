/**
 * DO NOT DELETE before Phase 5.
 *
 * Re-export shim for the legacy direct import path used by
 * `lib/hooks/use-realtime-voice.ts`. Real implementation lives at
 * `@/components/copilot/ai-voice-selector`.
 */
export {
  VOICES,
  AIVoiceSelector,
  type VoiceId,
} from '@/components/copilot/ai-voice-selector';
